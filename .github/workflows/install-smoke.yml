name: Install smoke

# Boots the actual OSS quickstart Docker stack, lets the live sim seed
# emergent data, and asserts the install works AND the seeded events are
# causally ordered. This is the gate that makes "install green on main"
# mean something — plain CI (cargo + bun) never boots the stack.
#
# Heavy by design: it compiles the whole workspace + SPA into the image,
# boots Postgres/NATS/all services, and runs the sim. ~25-35 min.
#
# NOTE: this is not yet a required status check — once it's proven
# stable, add "smoke" to main's branch-protection required checks so a
# broken install can't merge.

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

concurrency:
  group: install-smoke-${{ github.ref }}
  cancel-in-progress: true

jobs:
  smoke:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    defaults:
      run:
        working-directory: infra/oss-quickstart
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v4 (pinned per OpenSSF Scorecard)

      - name: Free up runner disk space
        # The image build compiles the full workspace; reclaim ~25 GB of
        # unused preinstalled toolchains so it doesn't run out of disk.
        working-directory: .
        run: |
          sudo rm -rf /usr/share/dotnet /opt/ghc /usr/local/.ghcup \
            /usr/local/lib/android /opt/hostedtoolcache/CodeQL
          df -h /

      - name: Set up Docker Buildx
        # docker-container driver — required for `cache-to: type=gha`
        # (the default docker driver cannot export remote cache).
        uses: docker/setup-buildx-action@bb05f3f5519dd87d3ba754cc423b652a5edd6d2c # v4.2.0 (pinned per OpenSSF Scorecard)

      - name: Build the quickstart image
        # Replaces `docker compose build` so the image's layers persist
        # across CI runs in the Actions cache. mode=max exports the
        # INTERMEDIATE stages too — that's where cargo-chef's cooked
        # dependency layer lives (see the Dockerfile) — so a warm run
        # skips straight to compiling workspace crates instead of
        # rebuilding the ~400-crate dep tree from scratch every time.
        # `load:` lands the image in the engine under the tag compose
        # expects (boss:latest); the boot step below runs --no-build so
        # exactly this image is what smoke tests.
        uses: docker/build-push-action@53b7df96c91f9c12dcc8a07bcb9ccacbed38856a # v7.3.0 (pinned per OpenSSF Scorecard)
        with:
          context: .
          file: infra/oss-quickstart/Dockerfile
          tags: boss:latest
          load: true
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Boot the stack
        run: |
          cp .env.example .env
          docker compose up -d --no-build

      - name: Wait for the sim to seed emergent data
        # The live sim runs on boot; poll until it has both closed Jobs
        # and issued invoices — proof the install booted AND the seed
        # path produced emergent state (not just empty tables).
        run: |
          set -euo pipefail
          for i in $(seq 1 60); do
            counts=$(docker compose exec -T postgres psql -U boss -d boss -At -F' ' -c \
              "select (select count(*) from jobs where closed_on is not null),
                      (select count(*) from invoices);" 2>/dev/null || echo "0 0")
            closed=$(echo "$counts" | awk '{print $1}')
            invoices=$(echo "$counts" | awk '{print $2}')
            echo "t=${i} closed_jobs=${closed:-0} invoices=${invoices:-0}"
            if [ "${closed:-0}" -gt 0 ] && [ "${invoices:-0}" -gt 0 ]; then
              echo "✓ emergent data present"
              exit 0
            fi
            sleep 10
          done
          echo "::error::sim did not produce emergent data within timeout"
          docker compose ps
          docker compose logs --tail=120 boss-services
          exit 1

      - name: Assert seeded events are causally ordered
        # The regression net for unrealistic event ordering. Runs the
        # always-valid ordering invariants (lifecycle dates ascend;
        # facts land within a gl_period) inside the postgres container.
        run: |
          set -euo pipefail
          docker cp ../lint/audit-ordering.sh "$(docker compose ps -q postgres)":/tmp/ord.sh
          docker compose exec -T \
            -e PGHOST=127.0.0.1 -e PGUSER=boss -e PGPASSWORD=boss -e PGDATABASE=boss \
            postgres bash /tmp/ord.sh

      - name: Gate on dispatcher dead-letters
        # A GATE since 2026-07-09 — the informational version let a
        # drain regression (#88: every produce side-effect starving,
        # 4,491 dead-letters over a full year) ride through CI, because
        # only the year-scale regen hard-failed on the count. The
        # tolerance exists for the honest transient: a short smoke
        # window can dead-letter a handful of events whose convergence
        # (production catching up to an early invoice) takes longer
        # than the redelivery budget. A systemic regression fires
        # hundreds — the threshold separates the two regimes; the
        # per-line tail below names the culprits when it trips.
        run: |
          n=$(docker compose logs boss-services 2>&1 | grep -c 'DEAD-LETTER' || true)
          echo "dispatcher dead-letters observed: ${n}"
          docker compose logs boss-services 2>&1 | grep 'DEAD-LETTER' | tail -10 || true
          if [ "${n:-0}" -gt 20 ]; then
            echo "::error::${n} dispatcher dead-letters (threshold 20) — a side-effect class is failing systemically, not transiently"
            exit 1
          fi

      - name: Tear down
        if: always()
        run: docker compose down -v || true
