-- =========================================================================
-- 08-gateway.sql — Gateway — no tables (WebAuthn credentials live in 10-people).
-- =========================================================================

-- (no objects in this module)
